import 'package:flutter/material.dart';

final ButtonStyle styButton = ButtonStyle(
  textStyle: MaterialStateProperty.all(
    TextStyle(fontSize: 20.0),
  ),
  backgroundColor: MaterialStateProperty.all(Colors.pinkAccent),
);
